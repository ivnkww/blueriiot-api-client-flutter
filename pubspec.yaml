name: blueriiot_api_client
description: Unofficial Dart/Flutter client for the Blue Riiot (Blue Connect) API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  aws_common: ^0.7.15
  aws_signature_v4: ^0.6.13
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
