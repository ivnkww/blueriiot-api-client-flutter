## 0.1.0

- Dart port of the JavaScript `blueriiot-api-client` package.
- Login via `user/login`, AWS SigV4 signed requests to the Blue Riiot API.
- All endpoints return raw `Map<String, dynamic>` JSON.
